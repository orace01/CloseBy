// Shared between the onboarding form (client) and its server action.
// Kept out of the "use server" file, which may only export async functions.
export const TONES = ["Professionnel", "Direct", "Chaleureux"] as const;
